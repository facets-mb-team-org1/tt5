import React from 'react';
import ReactDOM from 'react-dom';

// Mock component
const App = () => {
  return (
    <div>
      Mock React App
    </div>
  );
};

// Mock ReactDOM render
// Commenting out to prevent actual rendering
// ReactDOM.render(<App />, document.getElementById('root'));

console.log("Mock React index.js loaded");
